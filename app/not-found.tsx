/* 이 파일은 주소에 맞는 화면이 없을 때 Next 가 그리는 화면이다 */
import { NotFoundPage } from '@/views/not-found';

/* NotFound 는 Next 가 404 자리에 그리는 컴포넌트다 */
export default function NotFound() {
  return <NotFoundPage />;
}
